#ifndef ColorFormat_H    // To make sure you don't declare the function more than once by including the header multiple times.
#define ColorFormat_H

int justifyText();
int paddingZeros();
int numberFormatting();
int computingWNumbers();
#endif